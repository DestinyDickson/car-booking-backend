meta {
  name: Admin Login
  type: http
  seq: 6
}

post {
  url: http://localhost:3000/api/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "admin@example.com",
    "password": "admin123"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
